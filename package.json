{
  "name": "cf-vless",
  "version": "1.0.0",
  "description": "Cloudflare VLESS + WebSocket + TLS Worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
